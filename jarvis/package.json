{
  "name": "jarvis",
  "version": "1.0.0",
  "description": "JARVIS: a voice + vision assistant with a 3D second brain built from your Markdown notes, powered by Claude.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.mjs",
    "check": "node check.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  }
}
